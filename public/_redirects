# Toda rota entrega o index.html, e o roteador do navegador resolve.
#
# Sem esta linha, abrir /inicio ou /appmentoria/modulo/3 direto — ou
# recarregar a página numa dessas rotas — devolve 404, porque o arquivo
# não existe no disco.
#
# Antes havia só duas portas aqui e tudo o mais caía em 404. Bastava uma
# regra de endereço errada em algum painel de fora para a pessoa cair
# numa porta inexistente e ver uma tela preta sem explicação. Agora
# nenhum caminho leva ao vazio.
#
# Os arquivos de verdade (/assets/..., /sw.js, o manifesto, os ícones)
# são servidos antes desta regra, por existirem.

/*    /index.html   200
