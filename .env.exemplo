# Copie para `.env` e preencha. O `.env` não vai para o Git.
#
# As duas chaves abaixo são públicas por natureza: ficam no código que
# roda no navegador. A chave de serviço NÃO entra aqui, nem em lugar
# nenhum do front — ela só existe dentro da Edge Function.

VITE_SUPABASE_URL=https://crcclhmamknqkamvavyp.supabase.co
VITE_SUPABASE_ANON_KEY=
